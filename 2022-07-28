JavaScript에서 객체란 무엇일까?
-함수, 클래스(틀) => 객체, 개체, obeject
-틀로부터 개체(인스턴스)를 만들어서 사용
-객체를 만들어내는 틀의 역할을 하는 함수를 생성자 함수라고 한다
function 틀 () {} => new 틀() // 함수를 만들어서 틀을 만들고 틀로부터 new라는 키워드를 이용해서 객체, 즉 인스턴스를 생성
{} 객체 안에 사용할 데이터, 함수를 넣을 수 있다. 하나하나가 이름을 프로퍼티라 함

자바스크립트는 객체 기반 프로그래밍 언어이다. 자바스크립트를 구성하는 거의 '모든 것'이 객체이다
원시형 값을 제외한 나머지 값(함수, 배열, 정규 표현식 등)은 모두 객체이다.
원시형은 단 하나의 값만을 나타내지만 객체형은 다양한 값을 하나의 단위로 구성한 복합적인 자료구조이다.

객체는 0개 이상의 요소로 구성된 집합이다.
요소는 키와 값으로 구성된다.
자바스크립트에서 사용할 수 있는 모든 값은 프로퍼티(속성) 값이 될 수 있다.

프로퍼티 : 객체의 상태를 나타내는 값
메서드 : 프로퍼티를 참조하고 조작할 수 있는 동작

원시타입
- 원시 타입을 변수에 할당하면, 그 값이 바로 저장된다
- null을 제외한 원시타입의 종류는 typeof 연산자를 통해 확인 가능하다

참조타입
- 참조 값 : 참조 타입의 인스턴스(객체)
- 객체는 순서가 없는 프로퍼티로 이루어져 있음
- 프로퍼티의 값이 함수일 때 메서드라고 함
- 인스턴스화 : 객체를 만드는 것

